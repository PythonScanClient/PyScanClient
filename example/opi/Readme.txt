The Preference setting CSS Applications/Display/BOY/Runtime/PYTHONPATH needs to list

1) The top-level PyScanClient directory
2) The PyScanClient/example directory

Note that the preference setting uses '|' as a path separator, so the end result will look something like this:

/home/fred/PyScanClient|/home/fred/PyScanClient/example
